# ── IDE & OS ──────────────────────────────────────────────
*.iml
.DS_Store
Thumbs.db
Desktop.ini
/.idea
.kotlin

# ── Claude Code ───────────────────────────────────────────
.claude/settings.json
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock
.mcp.json
!plugins/*/.mcp.json
# Private session artefacts (CDI-safety: do not expose employer/maintainer identity)
.claude/handoff*.md
.claude/SESSION_*.md
.claude/SESSION-*.md
.claude/NEXT_SESSION_PROMPT.md
.claude/NOTICE-*.md
.claude/active-branches.md
.claude/audit-*.md
.claude/pixel9-*.md
.claude/website-review-*.md
.claude/launch.json
.claude/plans/
.claude/marketplace-submissions/
.claude/reports/
RERUN-CHECK.md

# ── Flutter tooling artefacts ─────────────────────────────
flutter/**/.dart_tool/

# ── Off-topic personal-portfolio code (NEVER in sceneview org) ──
hub-gateway/
hub-mcp/
mcp/docs/registry-submission-guide.md
mcp/docs/strategy-report.md
marketing/mcp-registry-submissions.md
marketing/articles/article-2-mcp-servers-3d.md
mcp-gaming/
mcp-interior/

# ── Gradle ────────────────────────────────────────────────
/local.properties
/.gradle
/build
/captures
.externalNativeBuild
.cxx
samples/*/build/
sceneview-core/build/
sceneview-web/build/
sceneview-desktop/build/

# ── Kotlin/JS ─────────────────────────────────────────────
kotlin-js-store/

# ── Node.js ───────────────────────────────────────────────
**/node_modules/
hub-mcp/package-lock.json

# ── MCP ───────────────────────────────────────────────────
mcp/.DS_Store
mcp/demo/*.mp4
mcp/dist/*.test.js
mcp/llms.txt
mcp/.mcpregistry_*
.mcpregistry_*

# ── Swift / Xcode ─────────────────────────────────────────
SceneViewSwift/.build/
SceneViewSwift/.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
samples/ios-demo/.build/

# ── Android artifacts ─────────────────────────────────────
*.apk
*.aab

# ── MkDocs ────────────────────────────────────────────────
docs/.cache/
docs/site/
site/

# ── Environment & secrets ─────────────────────────────────
.env
.env.local
.secrets/

# ── Signing & keystore artefacts (defense in depth) ───────
# The Play Store keystore is decoded transiently in CI (.github/workflows/play-store.yml)
# and rm'd in `if: always()`, but a workflow interrupted between decode and rm could
# leak it via failure artifacts. These patterns also block accidental `git add -A`
# after running release tooling locally.
**/*.keystore
**/*.jks
**/google-services.json
**/Secrets.xcconfig
**/*.p12
**/*.mobileprovision
# Exception: React Native debug keystore is the well-known shared dev key,
# safe to track and required for RN Android dev builds.
!samples/react-native-demo/android/app/debug.keystore

# ── Private / strategy ────────────────────────────────────
pro/
marketing/
branding/SECRETS.md
branding/SPONSOR_TIERS.md
.claude/handoff.md
.claude/branding.md

# ── Large media (binaries, not source) ────────────────────
/video/

# ── Shared asset catalog (downloaded via tools/download-assets.sh) ──
/assets/models/
/assets/environments/
/assets/textures/

# ── Platform demo assets (copied via .claude/scripts/sync-assets.sh) ──
# Note: android-demo-assets is NOT ignored (Play Asset Delivery, must be in git)
samples/android-tv-demo/src/main/assets/
samples/flutter-demo/models/
samples/react-native-demo/assets/models/
samples/react-native-demo/ios/Models/
samples/web-demo/src/jsMain/resources/models/
samples/web-demo/src/jsMain/resources/environments/

# Local MCP workspace artifacts (not canonical — canonical code is in mcp/packages/)
mcp-automotive/
mcp-healthcare/
mcp-gaming/
mcp-interior/
mcp-strategy-report.md

# AR test fixtures — actual MP4 recordings stay LOCAL only.
# Public fixtures must be vetted for PII (camera frames) before commit.
# Treat each MP4 like the device screenshots from the 2026-05-07 incident:
# fully audit content before promoting to a tracked file.
samples/android-demo/src/androidTest/assets/ar-recordings/*.mp4
